{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Batch Script",
            "type": "shell",
            "command": "${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],

}
